# 合并模块实施记录
依据用户指定的 DianXunZhiCe 结构执行，不创建 frontend，不覆盖团队仓库。
1. 保留 V0.3 算法；模型迁入 model，碳核算迁入 carbon，原模块通过兼容导入复用。
2. 编写 HTTP 契约回归测试，先验证新增 API 尚未存在。
3. backend 增加 routes/schema/service/config，统一响应；一体化接口分开公开电芯证据、上传质量和示例电池包决策。
4. 保存示例请求响应、字段单位、合并步骤；保留 V0.1/V0.2 小体积数据与哈希版本索引。
5. 运行原有测试、新接口测试、V0.3 数值复核，并在解压后的独立目录运行 smoke test。
